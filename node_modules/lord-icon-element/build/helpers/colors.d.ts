import { IColor } from '../interfaces.js';
/**
 * Parse color parameters to hex value.
 * @param r
 * @param g
 * @param b
 */
export declare function rgbToHex(value: IColor): string;
/**
 * Parse hex value to colors parameters.
 * @param hex
 */
export declare function hexToRgb(hex: string): IColor;
